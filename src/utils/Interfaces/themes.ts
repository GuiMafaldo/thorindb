export interface theme {
    background: string
    color: string
}

export const lightTheme: theme = {
    background: '#fff',
    color: '#000',
}
export const darkTheme: theme = {
    background: '#000',
    color: '#fff',
}
